import { personas, risks } from '../../data/demoExperienceData.js';

export default function DemoFinalResult({ sample, selectedRisks, selectedPersonas, onReset }) {
  const riskNames = risks.filter((item) => selectedRisks.includes(item.id)).map((item) => item.title);
  const personaNames = personas.filter((item) => selectedPersonas.includes(item.id)).map((item) => item.title);
  return <section className="demo-final-result" aria-labelledby="demo-result-title"><p>가상 사업 검증 결과</p><h3 id="demo-result-title" tabIndex="-1">{sample.title}</h3><div className="demo-final-result__status"><span>문서 구조화 <b>완료</b></span><span>법률·사업성 사전 검토 <b>완료</b></span><span>선택 페르소나 <b>{personaNames.join(', ')}</b></span></div><div className="demo-final-result__metrics"><span>핵심 항목 <b>12개</b></span><span>우선 확인 위험 <b>{riskNames.length}개</b></span><span>검증 과제 <b>{riskNames.length + 1}개</b></span><span>고객 질문 <b>6개</b></span></div><div className="demo-final-result__columns"><section><h4>다음 권장 행동</h4><ol>{sample.actions.map((action) => <li key={action}>{action}</li>)}</ol></section><section><h4>추천 고객 질문</h4><ul>{sample.questions.map((question) => <li key={question}>{question}</li>)}</ul></section></div><p className="demo-final-result__selected">선택한 검증 항목: {riskNames.join(', ')}</p><p className="demo-dashboard__notice">이 결과는 서비스 흐름을 설명하기 위한 가상 예시이며 실제 AI 분석 결과가 아닙니다.</p><div className="demo-action-row"><a className="landing-button landing-button--small" href="/auth/signup">내 사업계획서로 시작하기</a><button className="landing-text-button" type="button" onClick={onReset}>다른 샘플 체험하기</button></div></section>;
}
