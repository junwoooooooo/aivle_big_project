import { revisionLabel } from '../model/marketingContentModel.js';
export default function MarketingRevisionList({ revisions, activeNumber }) { return <section className="mk-panel mk-revisions" aria-labelledby="mk-revision-title"><div className="mk-panel__heading"><div><p>저장 이력</p><h2 id="mk-revision-title">Revision</h2></div></div><ol>{revisions.map((revision)=><li key={revision.revisionId} aria-current={revision.revisionNumber===activeNumber?'true':undefined}><strong>{revisionLabel(revision.revisionType)}</strong><span>{revision.origin==='AI'?'AI 생성':revision.origin==='USER'?'사용자 편집':'시스템 저장'}</span></li>)}</ol></section>; }
