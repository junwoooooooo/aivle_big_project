import{render,screen}from'@testing-library/react';import{MemoryRouter,Route,Routes}from'react-router-dom';import{describe,it,expect,vi}from'vitest';import BusinessPersonaIntegrationPage from'./BusinessPersonaIntegrationPage.jsx';
vi.mock('../hooks/useBusinessPersonaIntegration.js',()=>({default:()=>({loading:false,planning:{finalizedPlanning:{snapshotId:'final-1',displayLabel:'최종 확정 기획 — 가격·채널 조정'}},runs:[],busy:null,error:null,prepare:vi.fn()})}));
describe('BusinessPersonaIntegrationPage',()=>{it('shows three truthful external result areas and the probability warning',()=>{render(<MemoryRouter initialEntries={['/app/projects/1/business-persona-test']}><Routes><Route path="/app/projects/:projectId/business-persona-test" element={<BusinessPersonaIntegrationPage/>}/></Routes></MemoryRouter>);expect(screen.getByRole('heading',{name:'BM 분석'})).toBeInTheDocument();expect(screen.getByRole('heading',{name:'재무분석'})).toBeInTheDocument();expect(screen.getByRole('heading',{name:'페르소나 응답 테스트'})).toBeInTheDocument();expect(screen.getByText(/실제 시장 성공 확률/)).toBeInTheDocument();expect(screen.getAllByText('NOT CONNECTED')).toHaveLength(3);});});
