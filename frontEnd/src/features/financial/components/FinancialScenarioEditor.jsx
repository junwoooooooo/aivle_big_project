export default function FinancialScenarioEditor({ scenarios, onChange, disabled }) { return <section><h2>시나리오 비교</h2><p>판매량·가격·변동비·고정비 조정값을 실제 검토한 가정으로 수정할 수 있습니다.</p><div className="financial-scenarios">{scenarios.map((scenario, index) => <fieldset key={scenario.code}><legend>{scenario.label} 시나리오</legend>{[['salesVolumeAdjustment', '판매량'], ['priceAdjustment', '가격'], ['variableCostAdjustment', '변동비'], ['fixedCostAdjustment', '고정비']].map(([key, label]) => <label key={key}>{label} 조정(%)<input value={scenario[key]} inputMode="decimal" disabled={disabled} onChange={(event) => onChange(index, key, event.target.value)} /></label>)}</fieldset>)}</div></section>; }
